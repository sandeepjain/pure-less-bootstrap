pure-less-bootstrap
===================

Edit purecss files directly rather than overriding them in separate css file.

## Get Started
Clone this repo in your css (or css/libs) folder and get started. Put your own css modules in `modules` directory.

Compile `project.less` and you are ready to go.

